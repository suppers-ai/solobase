//! Table declaration for `suppers_ai__auth__api_keys`.
//!
//! Used by the `auth_ui` block's API-key endpoints. The repo currently
//! only owns the table name; row-level helpers live alongside the
//! callers in `auth_ui/api/api_keys.rs`.

pub(crate) const TABLE: &str = "suppers_ai__auth__api_keys";
