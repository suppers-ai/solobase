// OpenAI-compatible endpoints (Ollama, llama-server, LM Studio, Azure,
// Groq, Together, OpenRouter, ...). Reuses openai.rs for wire format;
// differs in that api_key is optional and /v1/models discovery can be sparse.
