package auth

import (
	"testing"
)

// Simple test to verify package compiles
func TestPackageCompiles(t *testing.T) {
	// This test just ensures the package can be compiled
	t.Log("Auth package compiles successfully")
}
