{
  "name": "solobase-cloud-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:cloud": "playwright test --project=cloud",
    "test:site": "playwright test --project=site",
    "test:api": "playwright test tests/api-flow.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
